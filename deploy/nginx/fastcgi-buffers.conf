# Inertia pages can emit a long Vite module-preload Link header.
# Keep enough headroom without changing request-body or upload limits.
fastcgi_buffer_size 32k;
fastcgi_buffers 16 32k;
fastcgi_busy_buffers_size 64k;
